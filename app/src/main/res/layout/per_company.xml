<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/halfNormal"
    app:cardCornerRadius="@dimen/normal"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/second">

        <ImageView
            android:id="@+id/detCompany"
            android:layout_width="0dp"
            android:layout_height="150dp"
            android:layout_margin="@dimen/halfNormal"
            android:contentDescription="@string/company_logo"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <TextView
            android:id="@+id/detCompanyName"
            style="@style/Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="9dp"
            android:padding="@dimen/halfNormal"
            android:textColor="@color/black"
            app:layout_constraintEnd_toEndOf="@+id/detCompany"
            app:layout_constraintStart_toStartOf="@+id/detCompany"
            app:layout_constraintTop_toBottomOf="@+id/detCompany"
            tools:text="Nama Perusahaan" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>